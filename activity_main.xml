<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <!-- linear layout contains the EditText, Button, and listView elements -->
    <LinearLayout
        android:layout_width="409dp"
        android:layout_height="729dp"
        android:gravity="center"
        android:orientation="vertical"
        tools:layout_editor_absoluteX="2dp"
        tools:layout_editor_absoluteY="0dp"
        tools:ignore="MissingConstraints">
        <!-- EditText element allows the user to input new items -->
        <EditText
            android:id="@+id/editText"
            android:layout_width="350dp"
            android:layout_height="75dp"
            android:ems="10"
            android:gravity="center"
            android:inputType="text" />
        <!--creates vertical space between the EditText and Button elements -->
        <Space
            android:layout_width="match_parent"
            android:layout_height="50dp" />
        <!-- Button element adds new items to the list -->
        <Button
            android:id="@+id/button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="Add" />
        <!--creates vertical space between the EditText and Button elements -->
        <Space
            android:layout_width="match_parent"
            android:layout_height="50dp" />
        <!-- This ListView element displays the list of items -->
        <ListView
            android:layout_width="wrap_content"
            android:layout_height="250dp"
            android:gravity="center"
            android:id="@+id/list_view"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
